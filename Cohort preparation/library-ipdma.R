# Library

library(dplyr); packageVersion('dplyr') #‘1.1.2’ #for data management
library(haven); packageVersion('haven') #‘2.5.2’ #for loading data from spss/stata
library(glue); packageVersion('glue') #1.6.2 #for formatting and interpolating strings
library(broom); packageVersion('broom') #1.0.4 #for tidying data
library(survival); packageVersion('survival') #3.5.3 #for cause-specific analysis
library(survminer); packageVersion('survminer') #0.4.9 #for cause-specific analysis
library(cmprsk); packageVersion('cmprsk') #2.2.11 #for subdistribution hazard analysis
library(riskRegression); packageVersion('riskRegression') #‘2023.12.21’ #for CIF plot
library(prodlim); packageVersion('prodlim') #‘2023.8.28’ #for Hist() function used in the CIF plot Section
library(ggplot2); packageVersion('ggplot2') #‘3.4.2’ #for CIF plot
library(adjustedCurves); packageVersion('adjustedCurves') #0.10.1 #for adjusted CIF plot
library(patchwork); packageVersion('patchwork') #1.3.0 #for combining plots
library(scales); packageVersion('scales') #1.2.1 #for colors
library(consort); packageVersion('consort') #1.2.0 #for Flowchart 
library(tableone); packageVersion('tableone') #‘0.13.2’
library(flextable); packageVersion('flextable') #‘0.9.1’
library(officer); packageVersion('officer') #‘0.6.2’

